<div class="container text-center">
  <div class="row justify-content-center">
    <div class="col-3 card" style="width: 18rem;">
      <div class="card-body">
        <h2 class="card-title" class="card-header">Leaderboard</h2>

        <div class="mb-3">
          <ul class="list-group list-group-flush">
            {{#each players as |player|}}
            <li class="list-group-item">{{player.username}}.....${{player.cash}}</li>
            {{/each}}
          </ul>
        <a href="/game" class="btn btn-primary mt-3">Play Again!</a>
        <button id="logout" class="btn btn-primary mt-3">Logout</button>
      </div>
    </div>
  </div>
</div>

<script src='/js/logout.js'></script>